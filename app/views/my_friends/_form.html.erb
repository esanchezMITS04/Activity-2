<%= form_with(model: my_friend, local: true) do |form| %>
  <% if my_friend.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(my_friend.errors.count, "error") %> prohibited this my_friend from being saved:</h2>

      <ul>
      <% my_friend.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :fullname %>
    <%= form.text_field :fullname %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.number_field :age %>
  </div>

  <div class="field">
    <%= form.label :birthdate %>
    <%= form.date_select :birthdate %>
  </div>

  <div class="field">
    <%= form.label :salary %>
    <%= form.text_field :salary %>
  </div>

  <div class="field">
    <%= form.label :motto %>
    <%= form.text_area :motto %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
